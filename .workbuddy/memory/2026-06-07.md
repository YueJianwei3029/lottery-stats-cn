# 2026-06-07 工作日志

## 前端测试环境搭建 — Phase 11 随机性检验集成

### 完成事项
1. **HTML** — 将第 4 个 Tab "高级统计"替换为"随机性检验"：
   - 桌面视图按钮 + 移动端底部导航同步更新
   - 新增控制栏（期数/α/起止日期输入 + 运行按钮）
   - 新增状态栏（loading/pass/fail/error 四种态）
   - 新增子按钮栏（摘要/比例检验/块内比例/游程检验/扑克检验/近似熵）
   - 新增视图容器（空态提示）

2. **CSS** — 深色主题适配全部随机性检验样式：
   - 控制栏（var(--bg-card) + border）、输入框（var(--bg-card2)）、子按钮（accent blue）
   - 状态栏/摘要卡片（green-soft 通过 / red-soft 失败）
   - 结果表格、检验详情卡片、图表容器
   - 移动端响应式（控制栏垂直排列、子按钮横向滚动）

3. **JS** — 新增 RandomnessModule 类（~230 行）：
   - 调用 /api/{彩种}/randomness 端点
   - 摘要视图：结论卡片 + 5×N 结果矩阵表格
   - 单项视图：表格 + 深色主题 p 值柱状图（ECharts，α 参考线红色虚线）
   - 参数变更自动失效缓存
   - 错误处理（网络/后端/参数）

4. **移除 demo_data.js** 引用（正式接入后端不再需要）

### 文件变更
- `other/frontend_test/index.html` — 替换 #viewAdvanced 区块
- `other/frontend_test/css/style.css` — 追加 ~160 行随机性检验模块样式
- `other/frontend_test/js/app.js` — 替换 loadAdvancedView() + 追加 RandomnessModule 类

### 图表间距修复
- 所有图表容器添加 `.chart-scroll` 包裹层，移动端 min-width: 560px + overflow-x scroll
- 热力图 grid.bottom 80→110，visualMap 下移
- 走势图 grid.bottom 72→98，dataZoom slider 上移
- 柱状图 grid 微调（58/56 bottom）

### 热力图优化
- y 轴标签：pos_front_1 → "前1" 等短标签（buildHeatmapYLabels 函数）
- 移除静态数字标签，改为 hover 显示（emphasis.label）
- 移除 y 轴名称 "位置"

### 16:18 合并到正式前端
- ✅ 原 frontend/ 已备份至 `frontend_backup_20260607/`
- ✅ `frontend_test/index.html` → `frontend/index.html`
- ✅ `frontend_test/css/style.css` → `frontend/css/style.css`
- ✅ `frontend_test/js/app.js` → `frontend/js/app.js`
- ✅ `config.js` / `echarts.min.js` 未变，`demo_data.js` 未带入
- ⚠️ 注意：正式前端不再需要 `demo_data.js`，已确认未复制
